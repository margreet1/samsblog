<!-- Home page -->
<%= render "partials/banner" %>

<div class="container">
    <%  @reviews.each do |review| %>
      <div>
        <%= render "partials/card" %>
        <div><%= image_tag review.imageUrl %></div>
        <div><%= link_to review.restaurant, review_path(review) %></div>
        <div><%= review.title %></div>
        <div><%= review.body %></div>
        <div><%= review.rating %></div>
      </div>
    <% end %>
  </div>

